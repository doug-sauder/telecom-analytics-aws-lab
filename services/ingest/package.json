{
  "type": "module",
  "name": "ingest",
  "version": "0.1.0",
  "description": "Ingest service for PM events",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src -e js --exec node src/server.js",
    "test": "NODE_OPTIONS='--experimental-vm-modules --no-warnings' jest --runInBand --testPathPattern=unit",
    "test:integration": "NODE_OPTIONS='--experimental-vm-modules --no-warnings' jest --runInBand --testPathPattern=integration",
    "test:all": "NODE_OPTIONS='--experimental-vm-modules --no-warnings' jest --runInBand"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "kafkajs": "^2.2.4",
    "pg": "^8.10.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@jest/globals": "^29.6.1",
    "jest": "^29.6.1",
    "nodemon": "^2.0.22",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
